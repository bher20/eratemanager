{
  "name": "eratemanager",
  "version": "0.3.1",
  "private": true,
  "description": "Energy Rate Manager - Go backend for electricity rate parsing and serving",
  "repository": {
    "type": "git",
    "url": "https://github.com/bher20/eratemanager.git"
  },
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version && node scripts/sync-version.js"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0"
  },
  "dependencies": {
    "@radix-ui/react-label": "^2.1.8",
    "class-variance-authority": "^0.7.1"
  }
}
